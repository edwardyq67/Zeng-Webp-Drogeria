// opennext.config.js
/** @type {import('opennext').Config} */
export default {
  buildCommand: "npm run build",
  // Cloudflare específico
  cloudflare: {
    // Configuración para Workers o Pages
  }
};